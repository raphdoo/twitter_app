mixin createPost(isLoggedInUser)
    .postFormContainer 
        .userImageContainer.image-avatar
            img(src= isLoggedInUser.profilePic, alt="user profile picture", srcset="")
        .textareaContainer 
            textarea#postText(placeholder="What's happening")
            .buttonContainer 
                button#submitTweet(disabled="") Tweet

mixin createPostReply(isLoggedInUser)
    #replyModal.modal.fade(tabindex='-1' role='dialog' aria-labelledby='replyModalLabel' aria-hidden='true')
        .modal-dialog(role='document')
            .modal-content
                .modal-header
                    h5#replyModalLabel.modal-title Reply
                    span(aria-hidden='true') &times;
                .modal-body
                    #originalPostContainer
                    .postFormContainer 
                        .userImageContainer.image-avatar
                            img(src= isLoggedInUser.profilePic, alt="user profile picture", srcset="")
                        .textareaContainer.postResponse 
                            textarea#replyTweet(placeholder="Reply Tweet")
                .modal-footer
                    button.btn.btn-secondary(type='button' data-dismiss='modal') Close
                    button#submitReplyButton.btn.btn-primary(type='button', disabled="") Reply
mixin createDeletePost(isLoggedInUser)
    #deletePostModal.modal.fade(tabindex='-1' role='dialog' aria-labelledby='deletePostModalLabel' aria-hidden='true')
        .modal-dialog(role='document')
            .modal-content
                .modal-header
                    h5#deletePostModalLabel.modal-title Delete 
                .modal-body
                    p Proceed to delete this tweet?
                .modal-footer
                    button.btn.btn-secondary(type='button' data-dismiss='modal') Close
                    button#deletePostButton.btn.btn-primary(type='button') Delete
